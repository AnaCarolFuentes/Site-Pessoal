#include <stdio.h>
#include <stdlib.h>

/*
Exercício 8.5 (KING, 2008): Escreva um programa que leia um array de inteiros de 10
posições e um valor a mais, que será usado para verificar se o mesmo existe no con-
junto fornecido. Como resultado do processamento, deve ser apresentado o primeiro
índice em que se encontrou o valor desejado. Para isso, implemente e utilize a fun-
ção int buscar( const int *a, int n, int chave ) . Essa função deve retornar
-1 caso o valor não seja encontrado.
*/

int buscar (const int *, int, int);

int main()
{

}

int buscar (const int *a, int n, int chave)
{

}